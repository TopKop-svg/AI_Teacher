# Конспект по Stream API (Java)

> Потоки (Streams) ‒ декларативный способ обработки коллекций c ленивыми вычислениями и возможностью параллельности.

---
## 1. Жизненный цикл Stream
1. **Источник данных** – коллекция, массив, генератор, файл…  
2. **Промежуточные операции (intermediate)** – преобразуют поток, *всегда* возвращают новый `Stream`, выполняются **лениво**.  
3. **Терминальная операция (terminal)** – закрывает цепочку, запускает вычисление и возвращает результат/побочный эффект.

![stream life-cycle](https://www.educba.com/academy/wp-content/uploads/2019/09/Thread-Life-Cycle-in-Java.png)<!-- autogenerated via mermaid -->

---
## 2. Создание потока
```java
List<Integer> list = List.of(1, 2, 3);
Stream<Integer> s1 = list.stream();
Stream<Integer> s2 = Stream.of(1, 2, 3);
IntStream range = IntStream.range(0, 10);
```

---
## 3. Частые операции
| Группа | Операции |
| --- | --- |
| *Промежуточные* | `filter`, `map`, `flatMap`, `distinct`, `sorted`, `peek`, `limit`, `skip` |
| *Терминальные* | `collect`, `reduce`, `forEach`, `min`, `max`, `count`, `anyMatch` / `allMatch` / `noneMatch` |
| *Collectors* | `toList`, `toSet`, `joining`, `groupingBy`, `partitioningBy`, `summarizingInt` … |

---
## 4. Примитивные стримы
`IntStream`, `LongStream`, `DoubleStream` – экономят память, избегают (un)boxing.

---
## 5. Параллельные потоки
* Используйте `parallelStream()` или `stream().parallel()`.
* Эффективны при CPU-bound задачах на *больших* коллекциях.
* Избегайте изменяемого общего состояния.

---
## 6. Полезные шаблоны
```java
// Top-K частых слов
List<String> top = Arrays.stream(text.toLowerCase().split("\\W+"))
        .filter(s -> !s.isBlank())
        .collect(Collectors.groupingBy(Function.identity(), Collectors.counting()))
        .entrySet().stream()
        .sorted(Map.Entry.<String, Long>comparingByValue(Comparator.reverseOrder())
                .thenComparing(Map.Entry.comparingByKey()))
        .limit(k)
        .map(Map.Entry::getKey)
        .toList();
```

---
## 7. Практика
Для закрепления реши методы в [`StreamTasks.java`](./StreamTasks.java) (базовый → средний → продвинутый уровень).

```bash
# Пример запуска
java Chapter_9.StreamTasks
```

Удачного стриминга! 🎉
